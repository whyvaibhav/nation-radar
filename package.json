{
  "name": "nation-radar-integrated",
  "version": "1.0.0",
  "description": "Nation Radar - Crestal Network Analytics Dashboard",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && echo 'Building with API URL:' $NEXT_PUBLIC_API_URL && npm run build",
    "start": "cd frontend && node server.js",
    "dev": "cd frontend && npm run dev",
    "force-rebuild": "echo 'Force rebuild $(date)' && cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
