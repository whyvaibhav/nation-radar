# Core dependencies for deployment
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Data processing (latest compatible versions)
pandas>=2.1.0
numpy>=1.26.0

# Add missing dependency
pyyaml>=6.0.1
